version: "3.3"

services:
  nginx:
    image: nginx:stable-alpine
    volumes:
      - ./docs:/usr/share/nginx/html
      - ./nginx.conf:/etc/nginx/nginx.conf
    deploy:
      labels:
        - "traefik.enable=true"
        - "traefik.http.routers.orconf.rule=Host(`orconf.wuhanstudio.uk`)"
        - "traefik.http.routers.orconf.entrypoints=web"
        - "traefik.http.services.orconf.loadbalancer.server.port=80"
        # TLS
        - "traefik.http.routers.orconfs.rule=Host(`orconf.wuhanstudio.uk`)"
        - "traefik.http.routers.orconfs.entrypoints=websecure"
        - "traefik.http.routers.orconfs.tls.certresolver=myhttpchallenge"
        # Redirect
        - "traefik.http.routers.orconf.middlewares=https_redirect"
        - "traefik.http.middlewares.https_redirect.redirectscheme.scheme=https"
    networks:
      - traefik-public
networks:
  traefik-public:
    external: true
